# Direct dependencies only — pip resolves the rest automatically.
# Install with:
#   pip install -r requirements_core.txt
#
# After a successful install, regenerate the full lockfile with:
#   pip freeze > requirements_lock.txt

--extra-index-url https://download.pytorch.org/whl/cu128

# PyTorch (CUDA 12.8)
torch==2.10.0+cu128
torchvision==0.25.0+cu128
torchaudio==2.10.0+cu128

# Computer vision / tracking
ultralytics
opencv-python
sahi
pillow

# Neuroscience
spikeinterface[full]
kilosort
neuroconv
ephyviewer
probeinterface
pynwb
neo
numba
mountainsort4
probeinterface

# Data / numerics
numpy>=1.24,<2   # 1.24 is the minimum for numba 0.64; stay on 1.x to avoid numpy 2 API breaks
scipy
pandas
matplotlib
networkx
shapely
polars
h5py
zarr<3             # zarr 2.x requires numcodecs<0.13; cbuffer_sizes was removed in numcodecs 0.13
numcodecs<0.13     # 0.13 dropped cbuffer_sizes from numcodecs.blosc, breaking zarr 2.x
faiss-cpu
scikit-learn
openpyxl

# GUI
PyQt6
pyqtgraph

# ML / deep learning extras
huggingface_hub

# Utilities
tqdm
psutil
requests
PyYAML
rich
